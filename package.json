{
  "name": "vaultkey",
  "version": "0.1.0",
  "description": "Secure password manager built on Electron",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "start:dev": "NODE_ENV=development electron ."
  },
  "devDependencies": {
    "electron": "^34.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}